collection = {1, 2, 3, 4, "hello", "world"}
print(collection)
print(type(collection))
#set is unordered, does not print duplicate values, set is mutable but the elements of set are immutable.

#empty set
empty = set()
print(type(empty))